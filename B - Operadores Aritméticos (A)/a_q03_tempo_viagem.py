# Entrada
distância = float(input('Distância em km: '))
velocidade = float(input('Velocidade média em km/h: '))

# Processamento
tempo = distância / velocidade

# Saída
print(f'A viagem demorará {tempo}h.')