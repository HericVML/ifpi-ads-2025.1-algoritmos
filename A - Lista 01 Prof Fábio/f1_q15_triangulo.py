# Entrada
base = float(input('Base do triângulo: '))
altura = float(input('Altura do triângulo: '))

# Processamento
area = (base * altura) / 2

# Saída
print(f'A área desse triângulo é {area}') # seja lá em que medida for.